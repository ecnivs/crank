# Gemini API Configuration
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Override default paths (these are fallbacks if not specified via CLI arguments)
# Note: CLI arguments (e.g., --path) take precedence over these environment variables
# PRESET_PATH=preset.yml
# PROMPT_PATH=prompt.yml
# SECRETS_PATH=secrets.json

# Optional: Override default log file
# LOG_FILE=crank.log
